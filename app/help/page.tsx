import { HelpContent } from '@/components/HelpContent';

export const metadata = { title: "Help Center", description: "Get help with Proposar." };

export default function HelpPage() {
  return <HelpContent />;
}
